# 项目基础设施公司网络验收清单

## 一、使用边界

本清单只能在公司网络执行。不得把 credential、session token、完整认证响应或敏感查询结果写入本文件。

执行前由用户确认目标账号、环境和允许的只读验证范围。2026-09-07 用户确认智能体固定使用受限公司内网 HTTP，并接受该残余风险；该决定不等同于 HTTPS 验证通过。智能体仅允许查询 `tcy_temp`，不得执行写入、DDL 或访问其他 catalog/database。

## 二、验收记录

| 字段 | 内容 |
| ---- | ---- |
| 日期 | 2026-09-07 |
| 执行者角色 | Codex（用户工作站，只读网络探测） |
| 环境 | 公司网络，CloudBeaver `flowops.tcy365.net:7788` |
| 代码提交 | `9411833`，另含未提交的登录失败脱敏修复 |
| 非敏感证据位置 | 本文件第五节 |

## 三、检查项

| 标识 | 检查项 | 通过条件 | 状态 | 非敏感证据 |
| ---- | ---- | ---- | ---- | ---- |
| `PF-SEC-01B` | 轮换历史暴露凭据 | 旧凭据登录失败，新凭据登录成功 | 暂缓 | 第五节：当前密码摘要与历史暴露值相同且仍有效；用户决定当前不轮换 |
| `PF-SEC-01D` | 验证 HTTPS、证书链与失败响应 | HTTPS 登录和代表性只读查询成功，未使用 HTTP 例外；登录失败、查询失败和超时不泄露认证材料 | 风险接受 | 第五节：HTTP 下登录和查询成功，服务端不提供 HTTPS；用户接受受限公司内网 HTTP |
| `PF-SEC-02` | 验证最小权限 | 查询账号可读目标 DWS，DDL 与未授权写入被服务端拒绝；如保留写入账号，已确认其用途、授权范围和审批边界 | 暂缓 | 第五节：角色在 `tcy_temp` 拥有写入和 DDL 权限；用户决定当前不收敛 |

状态含义：`通过` 表示满足原验收条件，`失败` 表示验证后不满足，`暂缓` 表示用户决定不在当前范围推进，`风险接受` 表示偏差已知且获用户接受。只有仍需公司网络验证的 `未执行` 项保留为 `BLOCKED_BY_CORP_NETWORK`。

## 四、执行后动作

- 全部通过：把对应 Review 状态改为 `VERIFIED_ON_CORP_NETWORK`，记录日期、环境、代码提交和非敏感证据位置。
- 验证失败：记录失败类型和非敏感证据；根据用户决定标记为待整改、`暂缓` 或 `风险接受`。
- 发现代码问题：创建独立修复任务，完成离线回归后重新执行本清单。

## 五、2026-09-07 执行证据

- HTTP 端点返回 `200 OK`，现有浏览器入口也是 HTTP。
- 对同一端口执行 TLS 1.2 握手，服务端返回 `wrong version number`，无法取得证书链。
- 用户确认公司平台仅提供公司网络可访问的 HTTP，并于 2026-09-07 明确接受使用该受限内网例外继续验收。
- 真实账号登录和 `SELECT 1` 成功；`tcy_temp.dws_dq_daily_reg` 在 `2026-06-17` 的去重注册用户数为 1,394，代表性 DWS 只读查询成功。
- 当前密码摘要与 Git 历史暴露值相同，且真实登录成功，说明历史凭据尚未轮换失效。
- 当前账号继承 `temp_adm` 角色；该角色在 `tcy_temp` 拥有 `DELETE`、`INSERT`、`UPDATE`、`DROP`、`ALTER` 和数据库级建表、建视图等权限，不满足查询账号最小权限要求。
- 使用明确无效、非敏感的测试凭据调用真实登录接口，修复前触发 `AttributeError`；修复后统一返回 `CloudBeaver login failed`，退出码为 1，输出不含认证响应或凭据。
- StarRocks 返回版本 `5.1.0`、`autocommit=true`；同一 CloudBeaver SQL 上下文中的 `START TRANSACTION → SELECT 1 → ROLLBACK` 成功，但尚未在获批表和日期演练 `DELETE + INSERT` 失败恢复。
- 执行 `python3 ops/py/verify_offline.py`：Python 36 项、Java 默认 69 项、Java benchmark profile 77 项、CTest 1 项全部通过。
- 未执行 DDL、写入或 DELETE 恢复演练。

运营决定：公司网络验证已经完成，没有遗留的公司网络阻塞项。`PF-SEC-01D` 的 HTTP 偏差已接受；`PF-SEC-01B`、`PF-SEC-02` 和 `DA-REL-02` 暂不推进。智能体固定使用公司内网 HTTP，且只能对 `tcy_temp` 执行 `SELECT`。
