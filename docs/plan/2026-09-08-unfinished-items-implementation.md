# 2026-09-07 交接剩余事项实施计划

> **For agentic workers:** 使用 `superpowers:executing-plans` 按任务执行；每项通过验收后再勾选。本文是实施计划，不代表已执行回填、联调或上线。

**Goal:** 闭环历史数据口径和线上事实，建立有版本证据的生产切换流程，再从真实任务提炼两个最小 Skill。

**Architecture:** 复用现有查询入口、人工回填脚本、算法测试和模拟器。按文档校正、数据修复、线上核实、条件式发布、真实任务积累五个工作包推进；外部依赖只阻塞对应工作包。

**Tech Stack:** Markdown、Python、StarRocks/CloudBeaver、现有 Java/C++ 算法与验证入口；不新增依赖或通用运行框架。

**Spec:** [未完成事项交接](../handoff/2026-09-07-unfinished-items.md)、[数据分析智能体规范](../spec/2026-09-04-data-analysis-agent-design.md)、[发牌算法智能体规范](../spec/2026-09-04-dealing-algorithm-agent-design.md)。发生冲突时，以交接中的最新决定及后续实际证据为准。

## 一、全局约束与已知证据

- 智能体联机取数仅允许 `tcy_temp` 的 `SELECT`，统一走 `ops/py/sr_exec.py`；临时 SQL 放 `ops/py/tmp/`。不得由智能体运行 `batch_insert_*`，包括本计划中的人工预览步骤。
- 公司内网 HTTP 风险已接受；凭据轮换、账号权限收敛、写入恢复演练仍暂缓，不重新设为本次交付门槛。
- 不重复修复交接已完成事项；`-66 / 75 / 112` 及双种子各 10 万局是交接记录的基线，不代表本次已验证实际部署。
- 算法修改沿用权威源仓先验证、再增量同步快照的流程；不静默修改 `algorithm/native/previous/`。
- 任何生产写入、部署或过滤开关切换，先形成具体可审阅产物，再由对应负责人执行或决定。
- 初始规划未执行查询；用户随后授权推进。实际执行范围与结果见第四节，不能将历史验证结果冒充本轮结果。

| 规划时本地依据（实施前状态） | 对计划的影响 |
| ---- | ---- |
| [Local] `docs/handoff/2026-09-07-unfinished-items.md:17` | DWS 回填在前，下游重算和结果更新在后 |
| [Local] `docs/handoff/2026-09-07-unfinished-items.md:25` | 配置/API 先核对实际部署，再决定是否修复 |
| [Local] `docs/handoff/2026-09-07-unfinished-items.md:33` | 新牌力公式必须记录第二个数据版本切点 |
| [Local] `docs/spec/2026-09-04-data-analysis-agent-design.md:13` | 阶段 B 状态过时，需要校正 |
| [Local] `docs/spec/2026-09-04-data-analysis-agent-design.md:238` | 旧文允许先建 Skill；按新交接改为先完成真实任务 |
| [Local] `starrocks/config/dq_game_room_config.md:50` | 文档声明为 DUPLICATE KEY，不能据此假设映射唯一 |
| [Local] `starrocks/config/dq_game_room_config.md:87` | 房间关联使用 `game_id + room_id`，不是仅 `room_id` |

维表文档未列有效期字段；这只能说明当前文档不足，不能证明线上不存在历史映射。实施时须查实际数据并取得配置变更证据。

## 二、顺序、责任与完成边界

| 顺序 | 工作包 | 主要执行方 | 开始条件 | 完成产物 |
| ---- | ---- | ---- | ---- | ---- |
| P0-1 | 文档状态校正 | 智能体 | 可立即执行 | Spec 与交接一致 |
| P0-2 | 历史数据修复与重跑 | 智能体准备/质检；数据负责人写入 | 公司网络可查询，人工写入窗口明确 | 分区验收证据及更新后的报告 |
| P0-3 | 线上配置、API、历史房间映射核实 | 智能体核对；研发/运维提供证据 | 可取得部署版本和历史配置 | 三项明确结论及证据 |
| P1-1 | 牌力版本切换 | 智能体准备候选；研发/数据负责人发布 | 公式、实际 ETL 入口、切换范围明确 | 版本切点、验证和发布记录 |
| P1-2 | 新过滤维度 | 产品决定；智能体实验；运维发布 | 明确启用维度、保护指标和预算 | 不启用决定，或标定与发布证据 |
| P2 | 真实任务与 Skill | 智能体执行；用户反馈 | 自然发生的真实需求 | 两套真实任务集，达标后创建 Skill |

P0-2 与 P0-3 可独立推进；历史数据修复不捆绑新版牌力或玩法 ETL 变更。P2 可以随真实任务积累，无需等所有生产切换完成，但不得用重复运行同一任务凑门槛。

## 三、任务清单

### 任务 1：同步规范状态

**文件：** 修改 `docs/spec/2026-09-04-data-analysis-agent-design.md`；必要时在原交接中追加完成日期与证据链接，保留历史语境。

- [x] 完整复读 Spec，将文档信息中的阶段状态改为“阶段 B 的只读联调已完成，其余验收项已暂缓或风险接受；阶段 C/D 未启动”。验证状态限定为只读联调，不能写成写入能力全面验收。
- [x] 同步第九、十一、十二节：拆分只读验证与暂缓项，记录 `PF-SEC-01B/02`、`PF-SEC-01D`、`DA-REL-02` 的既定状态。
- [x] 同步第六节任务表、第八节恢复描述、第十二节安全/回归边界：回填归人工运维；智能体只做准备和只读质检，不将恢复演练纳入自动分析流程。
- [x] 同步第十节创建门槛：先完成 5 至 10 个真实分析任务并证明流程稳定，再创建 Skill；消除第十节与阶段 C/D 的顺序冲突。
- [x] 检查文档差异和内部链接，确认不重新开启暂缓项、不新增写入授权。

**验收：** 各节状态、职责、门槛一致；历史背景可保留，但明确是历史问题，不表述为当前仍未修复。

### 任务 2：准备历史回填验收包

**文件：** 读取 `SQL_STYLE.md`、`ops/daily_data_ops.md`、`ops/py/first-classic-beginner/README.md`；完整读取实际使用的回填脚本及 SQL 后，新增 `ops/py/tmp/20260908_handoff_partition_quality.sql`。验收记录放 `docs/analysis/result/2026-09-08-historical-backfill-validation.md`。

- [x] 从 `dws_ddz_daily_game` 的 `2026-06-25` 至 `2026-07-01` 建立逐日基线：行数、关键缺失值、适用对局数。记录查询文件、执行时间和输出。
- [x] 追踪 `dws_app_allgame_stat` 的实际生产写入入口；核对 `batch_insert_allgame_stat.py` 与 `batch_insert_daily_allgame_stat.py` 的职责和调用，不凭文件名选脚本。
- [x] 确定下游全部受影响分区，依据旧口径生效区间和历史写入记录列出清单；不假设只有上游这七天。
- [x] 编写只读前后核验 SQL：逐日行数；源字段缺失对应的 `shuffle_times=-1`；适用玩法的四桶总和与同粒度对局数；异常 `bomb_bet` 单独计数。需要 raw 核对缺失来源时注明 DWS 无法回答的原因，并加分区过滤。
- [x] 在验收记录中列明人工预览和写入顺序、日期范围、预计分区、停止条件，交给数据负责人。

**人工操作：** 上游预览和回填沿用 README 已有命令，由数据负责人执行；本计划不执行。下游命令在完整读取对应脚本、核实实际参数后写入验收包，不猜参数。

**验收：** 形成明确的分区清单、可执行只读核验 SQL、人工操作单。若缺网络或写入窗口，仅标记对应依赖等待，不填造查询结果。

### 任务 3：人工回填后复验并更新分析

**文件：** 复用任务 2 的核验 SQL/记录；读取 `ops/py/first-classic-beginner/run_analysis.py` 全文及其实际 SQL；更新 `docs/analysis/result/first-classic-beginner-handcards-report.md`。

- [ ] 取得上游人工回填日志，以只读查询确认七个日期各自非零，缺失重洗次数仍为 `-1`；有一天失败就停止下游放行。
- [ ] 取得下游人工重算日志，按任务 2 清单检查四桶汇总。出现空值或异常倍数时先解释差额，不能强行把异常塞进无炸桶使总数相等。
- [ ] 确认分析使用历史 v2 窗口及 cohort 口径；不得意外替换成注册后真实首局口径。重跑前保留旧输出，避免覆盖唯一对照证据。
- [ ] 确认脚本只读取数路径后执行现有入口：`python3 -u ops/py/first-classic-beginner/run_analysis.py`。
- [ ] 在报告中显式记录总行数、非空手牌数、有效手牌数、非法行数及 E/G 排除数量，即使非法行数为零也写明。分别列分母及按日期/局序的覆盖率。
- [ ] 复核模块间人数/局数、桶汇总、输出文件及日志；只有窗口、样本定义和覆盖率均可解释时才比较新旧持有炸率，否则只报告当前结果和不可比原因。

**验收：** 每个目标分区、桶恒等式和手牌质量均有实际证据；报告关联实际查询、输出时间和人工操作记录。若仅运行现有脚本，不为此新增测试；若发现代码缺陷，先最小复现，再在对应现有测试文件增加一项业务回归。

### 任务 4：形成线上事实与历史映射结论

**文件：** 新增 `docs/analysis/result/2026-09-08-online-contract-verification.md`；读取实际部署证据及对应版本源码。本地线索为 `algorithm/native/previous/makedeal.json`、`ConfigManagerSys.cpp`、`algorithm/src/main/java/com/mamba/landlord/controller/ShuffleAndDealController.java`；它们不充当部署证明。

- [ ] 已取得用户对线上配置及 loader 源码的确认，并核对初始化入口、键名和读取路径；部署 commit/构建标识与运行时最终生效值仍待补齐。
- [x] 将 AC-ONLINE-01 判为“已证实故障”“已证实非故障”或“证据不足”；只有已证实故障才提出修复文件和最小回归，不因历史 Review 自动改代码。
- [x] 用户已确认 API 为翻译/重构发牌逻辑时的研究测试接口；核对现有点数响应和牌力、重洗字段，本次关闭生产出口契约改造，不把源码核查表述为运行时一致性验收。
- [x] 对 `dq_game_room_config` 检查 `game_id + room_id` 重复和属性冲突，补查跨游戏房间号复用；检查关联前后行数防止 join 放大。
- [ ] 获取历史改档与有效期证据，形成“游戏、房间、属性、有效起止、来源”的映射规则。没有可信历史时维持历史 ETL，不能把当前维表覆盖全历史；不创建未经需要证明的新表。

**验收：** 三项各自有结论、来源、时间和下一步。证据不足可以交付调查记录，但不算事实确认完成。涉及外部源码结论时，按 AGENTS.md 补实际调取列表、代码块及源码行号。

### 任务 5：单独准备牌力版本切换

**文件：** 读取 `algorithm/docs/deal-balancing-prd.md`、实际牌力公式来源及生产 ETL 全调用链；版本说明落 `docs/analysis/result/2026-09-08-card-power-version-transition.md`。精确代码修改清单在定位实际 ETL 后写入此记录，不预设当前批处理就是公式计算入口。

- [ ] 对照当前 ETL 与 PRD，固定旧/新公式和算法版本；选择覆盖普通手牌、炸弹、王炸、边界拆牌的确定性样例，保存输入及旧/新预期分数。
- [ ] 在实际实现位置形成最小候选改动和一项可运行的样例回归；若涉及 Java/C++ 共用规则，两端使用相同手牌向量验证。
- [ ] 准备受影响报表清单、上线范围、校验查询及恢复旧实现的步骤；历史数据保持原版本，历史重算须另列范围，不能默认为本次全量重算。
- [ ] 发布记录明确旧的 `2026-06-15` 切点与第二个实际生效切点，含 ETL commit、日期/时间、分区和版本。若同日混合版本，必须可按实际版本拆分，或协调在完整分区边界切换。
- [ ] 数据负责人执行后只读核验样例与分区；报表按版本分组，切点前后不直接比较牌力数值或分位桶。

**验收：** 候选实现有可复现分数证据；上线后另有实际切点与数据核验。交接计划不代替上线决定，也不把候选完成标记为生产已生效。

### 任务 6：按产品决定处理新增过滤维度

**文件：** 实验记录存 `docs/makedeal-strategies/2026-09-08-filter-dimensions-decision.md`；复用已有模拟器、配置及 PRD，不新建调参框架。

- [ ] 让产品/运维对潜在地主、地主优势、单牌数、炸弹数逐项记录“启用/不启用”，启用时明确适用房间、指标、联合拒绝率上限、触顶接受率上限和耗时预算。
- [ ] 若不启用，保留现有配置并以决定记录结项；不为未启用维度预先重标定。
- [ ] 若启用，先验证核心不变量，固定当前评分公式和配置版本，复用现有基线种子做同输入对比；已有基线只在公式和拆牌规则一致时复用。
- [ ] 保存每项拒绝率、联合拒绝率、触顶接受率、平均重洗次数及耗时；用独立种子复验候选。样本量从已有双种子各 10 万局方案起步，不把历史结果当作新维度验证。
- [ ] 达到产品保护阈值后形成候选配置与发布/回退记录，再由负责人决定上线；任一指标不达标则保留现有配置。

**验收：** 不启用有明确决定；启用有阈值、版本、样本量、种子、命令、指标和实际发布证据。涉及算法快照验证时使用 `python3 ops/py/verify_offline.py`，真实退出码与失败/skip 均留档。

### 任务 7：真实任务积累、反馈与最小 Skill

**文件：** 在两份现有 agent Spec 的阶段 C 追加任务索引，链接现有 `docs/analysis/result/`、`docs/makedeal-strategies/` 中的真实产物；达到各自门槛后才创建 `.agents/skills/data-analysis-agent/SKILL.md` 与 `.agents/skills/dealing-algorithm-agent/SKILL.md`。

- [ ] 数据方向积累 5 至 10 个真实任务，覆盖澄清、检索、只读 SQL、质检及留档；区分临时查询、分析方案与结果解释，保留真实遇到的失败案例，未发生的失败可用离线测试覆盖但不计真实任务数。
- [ ] 发牌方向积累至少 5 个真实任务，从正确性、策略比较、调参、公平性、性能、双语言同步中覆盖多类；每项有版本、配置、输入/种子、实际工具、验证和解释。
- [ ] 每个任务记录用户反馈为“接受/需修正/待反馈”，关联修订与复验结果；待反馈不计入反馈闭环完成数。
- [ ] 复核任务流程已经稳定后，分别创建仅引用既有规范和入口的最小 Skill；数据与发牌可独立达标，不互相等待。
- [ ] 用各自全部真实任务回放触发、流程与产物要求；对安全控制使用既有离线测试验证，不通过重放生产写入验收。

**验收：** 阶段 C 的数量、覆盖和反馈可追溯；阶段 D 的 Skill 无复制知识库、通用 Runtime 或新增确定性算法。现有实现和测试在移除 Skill 后仍有效。

## 四、阶段记录与本次检查

实施时每完成一个任务，在对应产物记录：完成内容、实际命令/证据、验收结果、剩余依赖。只有实际证据满足任务验收才勾选；“待人工执行”“待部署证据”“未启用”分别记录，不混写为通过。

首批执行建议为任务 1、任务 2 的准备部分、任务 4 的证据清单。取得人工回填和部署证据后推进任务 3；任务 5/6 独立发布，任务 7 随真实需求累计。

### 2026-09-08 推进结果

| 任务 | 实际状态 | 证据与剩余 |
| ---- | ---- | ---- |
| 1 规范同步 | 完成 | 数据分析Spec状态、人工边界及先C后D门槛已校正 |
| 2 回填准备 | 完成 | [验收包](../analysis/result/2026-09-08-historical-backfill-validation.md)：七日raw/DWS基线、190日炸弹桶对账、185日受影响分区、人工顺序及依赖核查；补修app源过滤并留回归 |
| 3 回填后分析 | 等待人工写入 | 未运行batch，未覆盖旧分析报告；462,732条缺失重洗次数默认值和185日旧桶尚未改库 |
| 4 线上事实 | 配置/API已确认；历史映射未完成 | [事实记录](../analysis/result/2026-09-08-online-contract-verification.md)：用户确认配置与源码均线上版本，配置键不匹配已证实；API为研究接口关闭契约改造；10803缺映射/有效期待补 |
| 5 牌力切换 | 前置定位完成，未发布 | ETL仅提取JSON分数；仍需完整上游评分/日志版本链及新旧预期分数，未假定上线日期 |
| 6 新过滤维度 | 待产品决定 | 维持现有配置；没有把待决定写成“不启用已批准”，没有启动新标定 |
| 7 真实任务/Skill | 未达门槛 | 本轮取数验收和配置核查可作为真实任务候选；未凑满任务数、未建立已通过的C回归集、未创建Skill |

验证：新增SQL的SQLite边界样例通过，主查询已在StarRocks真实运行。app范围修复先红后绿；Python测试共42项全部通过；`git diff --check`及本轮文档链接检查通过。算法未改，不重复Java/C++全量标定。本轮无数仓写入、DDL、算法配置发布、Git提交或推送。

执行中的范围调整：实际查询将allgame重算范围从七日样本扩展为连续185日；发现并修复了阻塞重算的跨app源过滤缺陷。日汇总/首日宽表通过四桶总和存在依赖，记录为人工下游核对范围，未自动扩大写入。
