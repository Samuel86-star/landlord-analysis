# 2026-09-07 未完成事项交接

> 本文只记录 2026-09-06 至 2026-09-07 算法与数据能力整改后的剩余事项。历史 Review 是当时快照，与本文冲突时以已验证源码和最新提交为准。

## 已完成基线

- 三带与四带二自吃、高置信拆牌短路、共享 `segmentResult` 已修复。
- 双种子各 10 万局标定已留档，生产配置为 `-66 / 75 / 112`；种子 `20260907` 全表已在 `algorithm/docs/deal-balancing-prd.md` 留档，不再是待办。
- `calcTotalHandScore` 已校验拆牌结果与整手牌的点数多重集；Java/C++ 保持同步。
- 炸弹桶已使用 `bomb_bet DIV 2`；非法 `hand_cards` 会记录数量并排除于模块 E/G，不再中断整批分析。
- 分析仓修复提交为 `9411833`，权威算法源仓提交为 `f2dbcf6`；统一离线验证退出码为 `0`。

## 待人工执行的数仓写操作

智能体联机取数仅允许对 `tcy_temp` 执行 `SELECT`，不得执行 `batch_insert_*` 或任何 `DELETE` / `INSERT`。以下必须由用户或数据负责人显式发起：

1. 按 `ops/py/first-classic-beginner/README.md` 复核 dry-run，回填 `dws_ddz_daily_game` 的 `2026-06-25` 至 `2026-07-01` 分区。
2. 在上游分区确认后，重算 `dws_app_allgame_stat` 受影响的历史分区，使新的无炸及 `1/2/3+` 炸弹桶口径真正落库。
3. 重跑 `first-classic-beginner`，重新生成分析结果；在覆盖率校验通过前，不横向比较新旧持有炸率。

验收条件：目标日期分区行数校验非零；缺失 `shuffle_times` 保留为 `-1`；各炸弹桶汇总与适用对局数一致；分析输出记录手牌覆盖率和非法行数。

## 待线上事实确认

- 核对实际部署的 `makedeal.json`、loader 键名和发牌入口版本，再判定 AC-ONLINE-01 是否为真实线上故障。
- 确认 `/api/shuffle` 是研究接口还是对局出口，再决定是否处理花色、牌力和重洗信息的响应契约。
- 在修改 `play_mode` ETL 前，先用 `tcy_temp.dq_game_room_config` 核对 `room_id` 唯一性、历史改档和有效期，避免用当前配置覆盖历史属性。

验收条件：留存线上版本或负责人确认证据；明确 API 定位与响应契约；得到可用于历史 ETL 的房间玩法映射规则。

## 待生产版本切换

- 数仓 ETL 尚未应用牌力 PRD 的新版公式；上线时必须记录继 2026-06-15 之后的第二个 `card_power` 版本切点，切点前后不得直接比较。
- 潜在地主、地主优势、单牌数和炸弹数四个过滤维度是否启用，仍需产品与运维明确决定；启用前必须重新标定并验证联合拒绝率和触顶接受率。

验收条件：记录 ETL 生效提交、上线日期和数据版本；若启用新过滤维度，保存阈值、样本、随机种子及拒绝率证据。

## 待智能体能力建设

- `data-analysis-agent` Skill 尚未创建；先积累 5 至 10 个真实分析任务，覆盖问题澄清、知识检索、只读 SQL、结果质检和结论留档，再提炼稳定流程。
- `dealing-algorithm-agent` Skill 尚未创建；先积累至少 5 个覆盖正确性、策略比较、调参、公平性、性能或双语言同步的真实任务。
- 两个方向的阶段 C/D、真实任务回归集和用户反馈闭环尚未实施。发牌方向在补齐任务编排、工具调用、结果解释和反馈闭环前，仍是算法服务而非完整智能体。

验收条件：真实任务记录达到门槛且重复交互流程稳定；Skill 只引用现有规范和入口，不复制知识库或新增通用 Agent Runtime。

## 待文档状态同步

- `docs/spec/2026-09-04-data-analysis-agent-design.md` 的阶段状态仍写“阶段 B 等待公司网络”，与当前决定不一致；后续维护该 spec 时应改为“阶段 B 的只读联调已完成，其余验收项已暂缓或风险接受；阶段 C/D 未启动”，并同步第九、十一、十二节的旧验收描述。

## 明确暂缓且不阻塞当前交付

- `PF-SEC-01B` 凭据轮换与 `PF-SEC-02` 账号权限收敛由用户决定暂不推进，状态为 `DEFERRED`。
- `PF-SEC-01D` 的受限公司内网 HTTP 风险已接受，状态为 `RISK_ACCEPTED`，不再要求 HTTPS。
- `DA-REL-02` 写入恢复演练不纳入当前范围，状态为 `DEFERRED`。
- 无 scorer 工厂的单路径按约定保留为降级路径。
- 四带二 helper 在当前生产拆牌顺序中仍是死代码，但自吃隐患已消除，暂不重构。
- 项目基础设施 Skill 明确不创建；CI 工作流在团队确认平台、成本、密钥边界和触发规则前暂不创建。

## 2026-09-08 推进补记

- 文档状态已同步；实施进度见[推进计划](../plan/2026-09-08-unfinished-items-implementation.md)。
- [历史回填验收包](../analysis/result/2026-09-08-historical-backfill-validation.md)已完成真实只读基线：七日462,732条源重洗次数提取缺失但未落 -1；炸弹桶需重算2026-03-01至09-01共185日，9月2日至6日已一致。已修复allgame脚本的跨app源过滤，尚未执行人工写入。
- 用户确认 `makedeal.json` 与 `zgdatbl.cpp` 对应线上配置/发牌源码，初始化读取键不匹配已证实；`/api/shuffle` 是研究测试接口，保留当前契约。房间10803历史映射及有效期仍待补齐，详见[事实记录](../analysis/result/2026-09-08-online-contract-verification.md)。
