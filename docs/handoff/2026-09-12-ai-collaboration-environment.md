# 2026-09-12 AI 协作环境检查与调整交接

> 本文记录本轮会话的事实、授权及交付状态，是交接材料，不新增全局规则或待执行授权。后续以用户当前请求、适用指令和实际文件状态为准；不要因本文存在而自动继续优化。

## 目标与授权

- 用户要求检查 AI 协作环境，先分析、不修改；重点识别规则冲突、重复、过宽触发和不必要流程，保留判断空间、授权边界、已有成果保护及如实报告。
- 首轮提出四项建议与小范围修改方案后，用户明确回复“确认，采纳建议”，随后完成下述两处修改。
- 用户之后询问还有哪些优化项，仅收到建议，未授权执行新增建议。
- 本次新授权仅为将本轮 handoff 保存到 `docs/handoff`；未请求提交或推送。

## 用户工作方式的证据与推测

- 明确要求：按需检查清单和少量记录，不遍历全部文件与历史；沟通偏好写成默认方式；满足任务后交付，不自行追加优化。
- 历史记录佐证：“分析742时间近一周数据”（任务 ID `01a08977-7bce-70a0-be61-a7058e8506bc`）中，用户自述数据分析师视角，追问低局深来自跨房还是整体参与少，并要求补充到原报告。
- 历史记录佐证：“诊断系统价值与机会发现”（任务 ID `01a08107-3719-7721-af15-8b5fb02f1d13`）中，用户要求按复杂度决定是否写 spec、是否使用 Agent 团队，并授权该次提交和推送。
- 推测而非已确认长期偏好：工作横跨游戏经营分析、数值与算法、AI 辅助开发及投资研究；重视机制解释、决策价值，愿意让 AI 判断技术路径。
- 不可推广：单次提交、推送授权不是长期授权；不能认定用户总要短回答、总不需要 spec。未写入额外用户画像或长期记忆。

## 检查范围与生效判断

- 当前是 Codex 桌面环境。运行指令标识 GPT-6；本地 `config.toml` 默认 `gpt-6-astra`、`medium`，未独立确认本任务是否存在覆盖设置。
- 已核对全局与项目 AGENTS，配置中相关插件启用情况；深入阅读 Superpowers 的部分流程 Skill、Ponytail 及 AI 系统演化 Skill。未遍历全部 Skill 正文或全部历史。
- Ponytail `full` 内容在会话中直接注入；其他 Skill 出现在清单不等于正文全部同时生效。
- 平台、应用与权限要求不能由本地规则覆盖。项目知识文档按任务引用；历史记录、插件缓存中的其他 AGENTS 和 README 不自动成为本项目指令。
- 未发现 `~/.codex/memories` 目录，但不能据此判断其他产品或账户没有记忆。
- 官方机制参考：[AGENTS 分层加载](https://learn.chatgpt.com/docs/agent-configuration/agents-md)、[Skill 按需加载](https://learn.chatgpt.com/docs/build-skills)。本轮结论基于本地规则文本与官方说明，不是第三方插件实现的全面源码审计。

## 已完成修改

### 全局 AGENTS

文件：[全局 AGENTS](/Users/maerun/.codex/AGENTS.md)。

- 新增三条协作默认：明确匹配才使用 Skill，按风险和不确定性选择并调整流程；已授权范围内推进，超范围再问；结论先行，分析按需展开，不固定行数。
- 合并原测试条款：复用项目测试体系，按行为与风险验证，Bug 优先补可复现检查；不因未按 TDD 顺序删除已有实现；保留失败、跳过和未验证项披露。
- 收窄外部审计入口：源码审计或实现级结论才适用严格源码证据要求；产品用法、能力和配置优先官方资料，不为格式扩大无关调查。
- 数据访问及多 Agent 段落未修改；原有安全、数据完整性和成果保护要求保留。

### Ponytail 本地缓存 Skill

文件：[Ponytail Skill](/Users/maerun/.codex/plugins/cache/ponytail/ponytail/4.9.0/skills/ponytail/SKILL.md)。

- 替换 `Output` 段落：取消代码优先、最多三行等固定限制，改为结论优先，按任务提供证据、机制和限制。
- 替换测试段落：取消只留一个检查、禁止框架与 fixtures 的限制，改为复用现有测试体系、按风险验证。
- 保留反过度工程、先理解再修改、输入验证、安全和数据保护原则。
- 未卸载或安装插件，未改 Superpowers 原始 Skill，未修改 `config.toml`、插件运行代码或项目 AGENTS。

## 备份与恢复

备份根目录：`/Users/maerun/.codex/backups/ai-collaboration-20260912-143538`。

| 原文件 | 对应旧版本 |
| ---- | ---- |
| `/Users/maerun/.codex/AGENTS.md` | `/Users/maerun/.codex/backups/ai-collaboration-20260912-143538/AGENTS.md` |
| `/Users/maerun/.codex/plugins/cache/ponytail/ponytail/4.9.0/skills/ponytail/SKILL.md` | `/Users/maerun/.codex/backups/ai-collaboration-20260912-143538/plugins/cache/ponytail/ponytail/4.9.0/skills/ponytail/SKILL.md` |

修改前已核对备份与原文件字节一致，修改后已回读核对内容。恢复时先比较当前文件与备份，保护后续新增修改；仅在用户授权恢复后将对应旧版本还原。不要直接覆盖之后的工作。

## 已验证与实际限制

- 已检查两份文件的修改差异，并验证全局数据访问、多 Agent 段落与备份一致。
- 本地注入器 `hooks/ponytail-instructions.js` 的 `getPonytailInstructions` 正常路径读取上述 `SKILL.md`。
- 用 Node 调用 `getPonytailInstructions`，分别检查 `lite`、`full`、`ultra`：包含新表达和测试条款，不含旧三行及禁止框架条款，仍保留输入验证原则。三种模式均通过。
- 这是注入内容检查，不是模型行为回归；没有运行代表性任务或压力场景实验，不能声称交付质量已经改善。
- 当前会话已注入的旧文本不会被文件修改撤回。新任务实际加载和指令冲突处理仍需验证。
- 注入器读取 Skill 失败时使用内置 fallback；该运行代码未修改，异常路径仍可能带旧条款。不要把正常路径验证扩大为全部路径验证。
- Ponytail 改动在版本缓存中，插件升级或缓存重建可能覆盖。未新增自动修复或监控。

## 未执行的后续建议

| 建议 | 已知问题与建议方向 | 状态 |
| ---- | ---- | ---- |
| Git 环境判断 | 项目 AGENTS 以 CLI/cowork 名称决定能否操作，`git remote -v` 无法证明写权限或推送能力；建议按授权、实际工具权限与仓库状态判断 | 仅建议，未授权修改 |
| CodeGraph 前置条件 | 全局 AGENTS 以 `.codegraph/` 存在作为必用条件；目录存在不保证索引有效或任务适合。建议跨模块定位优先有效索引，明确文件直接读，工具不可用直接降级 | 仅建议，未授权修改 |
| Superpowers 入口 | 仍有 1% 触发、强制审批和 TDD 重做等原始条款；本轮通过全局默认限定流程，未改原始 Skill | 先观察实际冲突，不自动扩改或卸载 |

以上属于条款局限或风险，未证实是过去延误、分析浅或交付不足的原因。没有建议靠修改规则解决模型能力、工具权限或材料不足的全部问题。

## 后续验收与停止条件

- 在真实小修中观察：是否在已授权范围内直接完成，复用现有检查，不机械新增 spec 或重复审批。
- 在真实数据分析中观察：是否提供机制、替代解释和口径限制，不受固定篇幅压缩。
- 在复杂任务中观察：是否按风险选择流程，并保护安全、数据、授权及已有成果。
- 用户未发起新任务或确认新增范围时，保持当前状态，不自动执行上表建议、不新增自动化、不继续追加优化。
- 写 handoff 前工作区已有未跟踪内容：`docs/analysis/result/room-742-weekly-2026-09-03-to-09-09.md` 和 `docs/analysis/room/`。本次未修改这些内容；不将其纳入本轮提交或清理。
