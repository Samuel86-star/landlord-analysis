# 项目基础设施审查（2026-09-04）

## 方向定义

项目基础设施为数据分析和发牌算法提供共享的安全、构建、测试、规范和可观测能力。它不是第三个业务智能体，不需要独立的智能体对话入口。

## 原始发现

### P0-1 CloudBeaver 凭据被提交且通过明文 HTTP 发送

**发现时证据**：2026-09-04 快照中的 `ops/py/sr_exec.py:23-26` 固定写入 HTTP 地址、用户名和密码摘要；`sr_exec.py:43` 直接向该地址发送请求；`sr_exec.py:55-60` 把凭据放入认证请求。以上行号仅属于 2026-09-04 快照，不代表当前代码位置或当前状态。

**影响**：仓库读者可能获得可重放认证材料；HTTP 链路上的凭据、查询和结果可能被监听或篡改。MD5 摘要在此处仍是登录凭据，不是安全脱敏。

## 当前处置状态

| 标识 | 事项 | 当前状态 | 当前证据或后续动作 |
| ---- | ---- | ---- | ---- |
| `PF-SEC-01A` | 移除当前源码中的 CloudBeaver 连接凭据 | `RESOLVED_OFFLINE` | 客户端从必填环境变量读取配置，缺失时在联网前失败 |
| `PF-SEC-01B` | 轮换历史提交中暴露的凭据 | `BLOCKED_BY_CORP_NETWORK` | 在公司账号系统轮换并验证旧凭据失效 |
| `PF-SEC-01C` | 默认拒绝 CloudBeaver 明文 HTTP | `RESOLVED_OFFLINE` | 默认要求 HTTPS，受限内网 HTTP 需显式设置例外变量 |
| `PF-SEC-01D` | 验证公司 HTTPS 端点与证书链 | `BLOCKED_BY_CORP_NETWORK` | 从公司网络完成真实登录和查询 |
| `PF-SEC-02` | 查询与写入账号最小权限 | `BLOCKED_BY_CORP_NETWORK` | 核对账号授权，证明日常查询账号不能执行高风险操作 |
| `PF-REL-01` | 脚本失败返回非零并停止下游 | `RESOLVED_OFFLINE` | Python 测试覆盖零行、异常结果和失败即停 |
| `PF-TEST-01` | Java 默认测试与 benchmark 分离 | `RESOLVED_OFFLINE` | 默认测试不执行 benchmark，显式 profile 执行 benchmark |
| `PF-TEST-02` | C++ 接入 CTest 且 Release 保留检查 | `RESOLVED_OFFLINE` | Release 构建后 CTest 能发现并执行测试 |
| `PF-GOV-01` | 审查发现生命周期 | `RESOLVED_OFFLINE` | Review 与索引采用状态字段和当前证据 |
| `PF-GOV-02` | 跨方向责任边界 | `RESOLVED_OFFLINE` | 按目录和职责定义维护者角色，不虚构具体人员 |

## 当前代码证据

- `ops/py/sr_exec.py:32-35,44-57,62-73,97-109`：连接配置从必填环境变量读取；默认拒绝 HTTP；登录失败不透传认证响应。
- `ops/py/test_sr_exec.py:24-96,99-147`：测试覆盖缺失配置在联网前失败、HTTP 显式例外和登录失败不泄露认证材料，以及 DDL 和多语句拦截。
- `algorithm/src/test/java/com/mamba/landlord/benchmark/ScoringAndSplittingBenchmarkTest.java:29`、`algorithm/src/test/java/com/mamba/landlord/benchmark/ShuffleAndScoringBenchmarkTest.java:25`：benchmark 使用 `@Tag("benchmark")` 标识，供默认测试与显式 benchmark profile 分离。
- `algorithm/native/CMakeLists.txt:15-20`：原生测试注册为 CTest 测试目标。
- `algorithm/native/test/main_test.cpp:9-15`：测试检查通过抛出异常实现，不依赖在 Release 中会移除的 `assert`。

## 公司网络阻塞项

以下事项只能按照[公司网络验收清单](../../tech/project-foundation-corp-network-checklist.md)在公司网络完成：

- `PF-SEC-01B`：轮换历史提交中暴露的凭据，并验证旧凭据失效。
- `PF-SEC-01D`：验证 HTTPS 端点、证书链、真实登录、只读查询和失败响应。
- `PF-SEC-02`：验证查询账号最小权限及服务端对 DDL 和未授权写入的拒绝。

## 责任边界

| 角色 | 负责范围 |
| ---- | ---- |
| 数据分析维护者 | `ops/py/`、`starrocks/`、数据知识与分析产物 |
| 发牌算法维护者 | 权威算法源仓、`algorithm/` 快照、模拟与算法测试 |
| 项目维护者 | 根目录规范、Review 生命周期、统一离线验证入口 |
| 公司系统管理员 | 凭据轮换、HTTPS 服务与账号最小权限 |

## 离线验证

- 执行日期：`2026-09-05`
- 命令：`python3 ops/py/verify_offline.py`
- Python：27 个单元测试通过；`compileall` 检查通过。
- Java：默认测试通过 62 个测试（不含 benchmark）；`benchmark` profile 通过 70 个测试（含 8 个 benchmark 测试）。
- C++：Release 配置和 `landlord_test` 构建通过；CTest 发现并通过 `1/1` 个测试。
- 最终结果：退出码 `0`，输出 `Offline verification passed.`。
- 边界：本次验证未访问 CloudBeaver 或 StarRocks；三个公司网络验收事项仍保持 `BLOCKED_BY_CORP_NETWORK`。

## 基础设施 Skill 决策

不创建 `project-foundation` Skill。安全拦截、退出码和测试门禁必须由代码、配置或 CI 等确定性控制执行；Skill 只能提醒模型，无法替代这些可靠控制。
