---
room_id: 420
房间名: 经典房（待填具体名）
玩法: 经典玩法（推断 ⚠️）
底分: （待填）
当前策略: new2（改造中）
MakeDealType: 1
状态: 改造中——makedeal.json 已将 420 由 old2 改为 new2，待灰度上线
---

# 房间 420 · old2 → new2（改造中）

> 420 原和其他经典房一样跑 `old2`（多炸）。现切换到 `new2 = with-pair [4,6,5,2,3] b14 sel17`，是继 742 之后第二个吃 Type1 降炸方案的经典房，也是**第一个用 `with-pair`（收对）均衡档**的房（742 用 `no-pair` 少炸档）。
> **指标口径（2026-08 重构）**：模拟器 `extracted/harness.exe`（C++ 真值）+ **搜索式最优拆牌** `optimal_split.h`（非贪心）；炸弹=持有、手数=人均最优手数。旧文档里贪心 Python 模拟的 0.207/手数 6.17 已作废。

## 改造后发牌参数（new2）

> 来源：`makedeal.json` → `MakeDealStrategy.new2`

| 参数 | 值 |
|---|---|
| MakeDealType | 1（Type1，CouPaiStrategy 拼牌） |
| BeginMakeNum | 14 |
| BeginSelectBanker | 17 |
| CouPaiStrategy | `[[4,6,5,2,3]]`（顺→飞机→连对→对→三，**收对、不含炸码13**） |
| TargetValue | 999（恒拼牌） |
| TargetRound | 10 |
| BigCardsTo | 4（Type1 惰性） |
| FirstChairHandCount / BombCount / BigCardsCount | 5 / 2 / 5（Type1 惰性） |
| OtherChairHandCount / BombCount / BigCardsCount | 6 / 2 / 4（Type1 惰性） |

> 即模拟器里的 `with-pair [4,6,5,2,3] b14 s17 tv999`。`b14 sel17 = 14 随机 + 3 张拼满`，干预较轻。

## 指标对比（最优拆牌模拟，N=20000，三家同策略）

| 指标 | 改造前 old2 | **改造后 new2** | 纯随机基线 | 解读 |
|---|---|---|---|---|
| 持有炸(人均) | 0.419 | **0.140** | 0.189 | 砍半多、贴自然（=线上 0.138 ✓） |
| 单局炸弹率 | 0.701 | **0.360** | 0.461 | 略低于自然 |
| 0/1/2/3+炸密度 | 0.30/0.33/0.22/0.14 | **0.64/0.31/0.05/0.01** | 0.54/0.37/0.08/0.01 | 0 炸局从 30%→64% |
| 人均最优手数 | 5.93 | 6.16 | 7.34 | 持平 old2、远顺于自然 |
| 人均散牌 | 2.55 | 2.65 | 3.83 | 少于自然 |
| 首叫诱导度 | 1.256 | 1.254 | 1.328 | 均衡 |
| 抗衡度 | 1.417 | 1.424 | 1.299 | 农民能抗 |
| 牌力 V | 14.6 | 15.2 | 0.8 | — |

> 模拟器：`extracted/harness.exe --type1 --coupai 4,6,5,2,3 --begin 14 --select 17 --tv 999 --tr 10 -n 20000 --seed 1 --reals 3`
> 线上实测待灰度后补：`dws_ddz_daily_game.bomb_cnt`（持有）+ `bomb_bet`（打出，≠持有），对照改造前后窗口。

## 在新口径下的定位与改造建议

- **现排 Type1 第 31/64**（得分 0.791）：降炸达标（持有 0.140）、手顺均衡，是好配置。与 742(`new`) 同档（742 第 26），两者持有炸几乎相同（0.140 vs 0.139）。
- **`with-pair b14s17` 不在新 TOP20**：因干预最轻，单局炸率 0.360 低于自然 0.461，按"低等级**趋近纯随机**"理念略偏减炸。若要更贴自然：
  - Type1 TOP1 = `bomb-last [4,6,5,3,2,13] b11 s15 tv999`（单局炸率 0.454 ≈ 自然，手数 5.51，得分 0.913）；
  - 或 `with-pair b8 s14`（Type1 第 10，单局炸率 0.397、手数 5.72）——同 `with-pair` 家族里更贴自然的档。
- 完整 TOP20 见 [`algorithm/native/extracted/top20_report.md`](../../../algorithm/native/extracted/top20_report.md)，可落地 JSON 见 `top20_configs.json`。

> ⚠️ `docs/tech/classic-makedeal-config-topn.md` 把 `new2` 列为"Type1 #1"是**旧贪心拆牌**口径，已失效；新口径下 `new2` 排 31/64，以 `extracted/top20_report.md` 为准。

## 备注

- `new2` 策略定义已加进 `makedeal.json`（工作区改动，上线时机由研发定）。
- 对照参照 742（已上线 `new`，线上 `bomb_bet` 有炸局 67%→32%）。
- 关联：源码逆向 [`../742-420-reverse-analysis.md`](../742-420-reverse-analysis.md)、memory `project_makedeal-debomb-plan-2026-08`。
- 玩法归属"经典玩法"为推断（未查 `dq_game_room_config` 确认）。
