---
room_id: 742
房间名: （待填）
玩法: 经典玩法（推断 ⚠️）
底分: （待填）
当前策略: new
MakeDealType: 1
状态: 已灰度上线（2026-07-31）
---

# 房间 742 · new 策略

> 本房间是**唯一跑 `new` 策略**的经典房，也是目前唯一有"改造前后线上实测数据"的房（见下），可作其他经典房改造的参照样板。
> **指标口径（2026-08 重构）**：模拟器换成 `extracted/harness.exe`（C++ 真值，发牌管线 1:1 复刻线上）+ **搜索式全局最优拆牌** `optimal_split.h`（min-combo → max-Σscore，非贪心）。炸弹口径=**持有**（物理四张/王炸）；手数=**人均最优手数**。旧文档里贪心 Python 模拟的 0.161/手数 6.86(t0拆) 已作废。

## 当前发牌参数

> 来源：`makedeal.json` → `MakeDealStrategy.new`

| 参数 | 值 |
|---|---|
| MakeDealType | 1（Type1，CouPaiStrategy 拼牌） |
| BeginMakeNum | 11 |
| BeginSelectBanker | 15 |
| CouPaiStrategy | `[[4,5,3,6]]`（顺→连对→三→飞机，**不收对、不含炸码13**） |
| TargetValue | 999（恒拼牌） |
| TargetRound | 10 |
| BigCardsTo | 4（Type1 惰性） |
| FirstChairHandCount / BombCount / BigCardsCount | 5 / 2 / 5（Type1 惰性） |
| OtherChairHandCount / BombCount / BigCardsCount | 6 / 2 / 4（Type1 惰性） |

> 即模拟器里的 `no-pair [4,5,3,6] b11 s15 tv999`。

## 现状指标（最优拆牌模拟，N=20000，三家同策略）

| 指标 | 742(new) | 纯随机基线 | old2(Type0) | 解读 |
|---|---|---|---|---|
| 持有炸(人均) | **0.139** | 0.189 | 0.419 | 贴自然、远低于 old2（=线上 0.137 ✓） |
| 单局炸弹率 | 0.354 | 0.461 | 0.701 | **略低于自然**——轻度减炸 |
| 0/1/2/3+炸密度 | 0.65/0.30/0.05/0.01 | 0.54/0.37/0.08/0.01 | 0.30/0.33/0.22/0.14 | 0 炸局占 65% |
| 人均最优手数 | 6.00 | 7.34 | 5.93 | 比自然顺约 1.3 手 |
| 人均散牌 | 2.97 | 3.83 | 2.55 | 少于自然、牌成型 |
| 首叫诱导度 P_max/P_avg | 1.243 | 1.328 | 1.256 | 比自然更均衡 |
| 抗衡度 (P_mid+P_min)/P_max | 1.443 | 1.299 | 1.417 | 农民更能抗、悬念足 |
| 牌力 V | 19.3 | 0.8 | 14.6 | — |

> 模拟器：`extracted/harness.exe --type1 --coupai 4,5,3,6 --begin 11 --select 15 --tv 999 --tr 10 -n 20000 --seed 1 --reals 3`
> P = sigmoid(牌力/40)（归一化非负强度，用于诱导/抗衡比值）。

## 线上实测（打出 `bomb_bet`，2026-07-31 切换前后）

| 指标 | 改造前(old2) | 改造后(new) | 备注 |
|---|---|---|---|
| `bomb_bet` 有炸局占比 | 67% | **32%** | 打出口径 ≠ 持有 |
| 局级炸弹期望 | 1.14 | **0.39** | 单家 ≈0.38→0.13 |
| 0 炸局占比 | 33% | **68%** | |
| ≥2 炸局占比 | 31% | **5%** | |
| 真人 `card_power_final` 均值 | 25.9 | 27.7 | 分布右移（<-5 桶 15.4%→11.9%、≥35 桶 34.0%→36.0%） |
| 机器人牌力 | -4.3 | -5.3 | 略降 |
| 真人胜率 | 49.9% | 49.3% | 持平 |

> 改前窗口 07-24~07-30，改后 07-31~08-03；`card_id`/`shuffle_type` 不变 → 改的是底层做牌逻辑。对照房 420/4484/12074 同期无变化。

## 在新口径下的定位与改造建议

- **现排 Type1 第 26/64**（得分 0.804）：手顺、均衡、持有炸贴自然，已是好配置；但**单局炸率 0.354 低于自然 0.461**——按"低等级房炸弹维度**趋近纯随机**"的理念，742 **轻度减炸过头**。
- **若要更"抱随机"**：Type1 TOP1 = `bomb-last [4,6,5,3,2,13] b11 s15 tv999`（单局炸率 0.454 ≈ 自然 0.461，手数 5.51、抗衡 1.472），得分 0.913。其 CouPai 在 742 的 `[4,5,3,6]` 基础上**加收对(2)+末位炸码(13)**，让炸弹分布更贴自然同时更顺。
- 完整 TOP20 见 [`algorithm/native/extracted/top20_report.md`](../../../algorithm/native/extracted/top20_report.md)（最优拆牌+严苛口径），可落地 JSON 见 `top20_configs.json`。

> ⚠️ `docs/tech/classic-makedeal-config-topn.md` 是**旧贪心拆牌**的 TOP 排名，已与本文件口径不一致，以 `extracted/top20_report.md` 为准。

## 备注

- 742 是 `new` 策略唯一使用者；其余经典房多为 `old2`（持有炸 ~0.42，待降）。
- 关联：源码逆向 [`../742-420-reverse-analysis.md`](../742-420-reverse-analysis.md)、审计 [`../makedeal-code-quality-audit.md`](../makedeal-code-quality-audit.md)、memory `project_makedeal-debomb-plan-2026-08`（含 742 实测切点明细）。
- 持有口径用 `dws_ddz_daily_game.bomb_cnt`；打出用 `bomb_bet`（注意 ≠ 持有）。
- 玩法归属"经典玩法"为推断（未查 `dq_game_room_config` 确认）。
