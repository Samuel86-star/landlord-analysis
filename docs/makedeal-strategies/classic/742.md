---
room_id: 742
房间名: （待填）
玩法: 经典玩法（推断 ⚠️）
底分: （待填）
当前策略: new
MakeDealType: 1
状态: 已灰度上线（2026-07-31）
---

# 房间 742 · new 策略

> 本房间是**唯一跑 `new` 策略**的经典房，也是目前唯一有"改造前后实测数据"的房（见下）。可作其他经典房改造的参照样板。

## 当前发牌参数

> 来源：`makedeal.json` → `MakeDealStrategy.new`

| 参数 | 值 |
|---|---|
| MakeDealType | 1（Type1，CouPaiStrategy 拼牌） |
| BeginMakeNum | 11 |
| BeginSelectBanker | 15 |
| CouPaiStrategy | `[[4,5,3,6]]`（顺→连对→三→飞机，**不收对、不含炸码13**） |
| TargetValue | 999（恒拼牌） |
| TargetRound | 10 |
| BigCardsTo | 4 |
| FirstChairHandCount / BombCount / BigCardsCount | 5 / 2 / 5 |
| OtherChairHandCount / BombCount / BigCardsCount | 6 / 2 / 4 |

> 即模拟器里的 `no-pair [4,5,3,6] b11 sel15`。

## 现状指标

| 指标 | 模拟器预测（持有） | 线上实测（打出 `bomb_bet`） | 备注 |
|---|---|---|---|
| 炸弹 | 0.161（贴自然 0.191） | 局级期望 1.14→**0.39**（单家 ≈0.38→0.13） | 持有 ≠ 打出 |
| 有炸局占比 | — | 67%→**32%** | |
| 0 炸局占比 | — | 33%→**68%** | |
| ≥2 炸局占比 | — | 31%→**5%** | |
| 手数 | 6.86（t0拆） | — | |
| 庄闲炸差 | +0.017 | — | 公平 |
| 单牌 / 庄闲大牌差 | 4.83 / +0.02 | — | 牌偏散 |

> 模拟器：`py -3 algorithm/native/old2_type1_sim.py run --coupai no-pair --begin 11 --select 15 --tv 999 --tr 10`
> ⚠️ `bomb_bet`（打出）≠ 模拟器持有炸弹（0.161）；持有口径用 `dws_ddz_daily_game.bomb_cnt`。

## 改造（已上线，2026-07-31）

742 已上线一次配牌调整（核心目标：减炸 + 手牌整洁，不靠给炸弹）。改造效果（线上实测）：

- **炸弹大降**：`bomb_bet` 有炸局 67%→32%，局级期望 1.14→0.39。
- **真人牌力略升**：`card_power_final` 均值 25.9→27.7、分布右移（<-5 桶 15.4%→11.9%、≥35 桶 34.0%→36.0%）。
- **机器人牌力略降**：-4.3→-5.3。
- **真人胜率持平**：49.9%→49.3%。
- `card_id` / `shuffle_type` 不变 → 改的是底层做牌逻辑、非牌库配置。
- 对照房 420 / 4484 / 12074 同期无变化。

> 改前窗口 07-24~07-30，改后 07-31~08-03。

## 备注

- 742 是 `new` 策略唯一使用者；其余经典房多为 `old2`（现状 ~0.43 炸，待降）。
- 关联 memory：`project_makedeal-debomb-plan-2026-08`（含 742 实测切点明细）。
- 玩法归属"经典玩法"为推断（未查 `dq_game_room_config` 确认）。
